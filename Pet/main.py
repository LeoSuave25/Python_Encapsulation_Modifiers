from userinteract import UserInteraction
from style import Styling

headers = Styling()

ui = UserInteraction()
# Use the UserInteraction object to prompt the user and display the pet's info
ui.retry()